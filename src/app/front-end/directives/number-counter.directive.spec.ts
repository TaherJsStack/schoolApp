import { NumberCounterDirective } from './number-counter.directive';

describe('NumberCounterDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberCounterDirective();
    expect(directive).toBeTruthy();
  });
});
