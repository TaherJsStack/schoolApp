import { LoadingScreenDirective } from './loading-screen.directive';

describe('LoadingScreenDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadingScreenDirective();
    expect(directive).toBeTruthy();
  });
});
